services:
  - type: web
    name: rag-assistant
    env: node
    buildCommand: npm install && npm run ingest
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /api/health
